"""Evaluation harness: routelet vs the LLM baseline.

Runs both routers over a held-out test set and reports accuracy / F1,
latency (p50/p95), and cost-per-1k, then prints a comparison table. This
is the part that proves the fine-tuned router is worth using.
"""

# TODO: run routelet and the baseline over the test set, compare quality/latency/cost.
