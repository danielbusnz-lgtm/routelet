"""Fine-tune a small model to classify text into intents.

Loads the labeled data from ``data.py``, fine-tunes a small base model
(classifier head or a small generative model via LoRA), and saves the result.
"""

# TODO: load base model, fine-tune on the labeled split, save the model/adapter.
