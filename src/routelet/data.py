"""Dataset loading and the label schema for intent routing.

Reads a JSONL file of ``{"text": ..., "intent": ...}`` rows and produces
train/test splits the trainer can consume.
"""

# TODO: load examples/*.jsonl, validate the intent labels, split into train/test.
